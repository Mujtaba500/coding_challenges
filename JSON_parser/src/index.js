// import { dirname } from "path";
// import { fileURLToPath } from "url";

import { readFile, readDirectory } from "./helpers.js";

// Current directory
// const __dirname = dirname(fileURLToPath(import.meta.url));

// Current File
// const filename = fileURLToPath(import.meta.url);

// Step 1
readDirectory(
  "/home/mujtaba/code/projects/coding_challenges/JSON_parser/tests/step1",
  "step1"
);
